[project]
name = "belton-ai-market-radar"
version = "0.1.0"
description = "Belton AI Market Radar"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "streamlit",
    "yfinance",
    "pandas",
    "numpy",
]

[tool.uv]
package = false
